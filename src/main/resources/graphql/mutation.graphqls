type Mutation {
    register(userCreate: UserCreate): UserView!
    login(loginCredentials: LoginCredentials): AuthenticatedUser!
    forgotPassword(email: String): Boolean
    resetPassword(token: String, password: String): Boolean
}